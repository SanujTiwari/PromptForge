// PromptForge - Prisma Seed File
// Will be populated in Module 2 (Database)

async function main() {
  console.log('🌱 Seeding database...');
  // Seeds will be added when models are created
  console.log('✅ Seeding complete.');
}

main()
  .catch((e) => {
    console.error('❌ Seed failed:', e);
    process.exit(1);
  });
