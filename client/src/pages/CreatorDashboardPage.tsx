import { ArrowRight, BarChart3, CircleDollarSign, FilePlus2, Star } from 'lucide-react';
import { Link } from 'react-router-dom';
import { prompts } from '@/data/catalog';

const stats = [{ label: 'Revenue this month', value: '₹12,480', icon: CircleDollarSign, note: '18% from last month' }, { label: 'Prompts in use', value: '4', icon: FilePlus2, note: '1 awaiting review' }, { label: 'Average rating', value: '4.8', icon: Star, note: '58 responses' }];
export default function CreatorDashboardPage() { return <div className="page-shell py-10"><div className="flex flex-col justify-between gap-5 border-b border-paper-300 pb-7 sm:flex-row sm:items-end"><div><p className="eyebrow">Creator workspace</p><h1 className="display mt-2 text-5xl">Good morning, Mina.</h1><p className="mt-3 text-sm text-ink-500">Your shelf is doing the quiet work.</p></div><Link className="button-primary" to="/seller/prompts/create"><FilePlus2 className="h-4 w-4" />New prompt</Link></div><section className="grid border-l border-t border-paper-300 sm:grid-cols-3">{stats.map((stat) => <div key={stat.label} className="border-b border-r border-paper-300 bg-white p-5"><stat.icon className="h-5 w-5 text-forge-600" /><p className="mt-9 text-xs font-bold uppercase tracking-[.14em] text-ink-500">{stat.label}</p><p className="display mt-2 text-4xl">{stat.value}</p><p className="mt-2 text-xs text-moss-700">{stat.note}</p></div>)}</section><section className="mt-10 grid gap-8 lg:grid-cols-[1.3fr_.7fr]"><div><div className="flex items-end justify-between"><div><p className="eyebrow">Your listings</p><h2 className="display mt-2 text-3xl">What people return to.</h2></div><Link className="button-text" to="/seller/prompts">Manage all <ArrowRight className="h-4 w-4" /></Link></div><div className="mt-5 divide-y divide-paper-300 border-y border-paper-300">{prompts.slice(0, 3).map((prompt, index) => <div key={prompt.slug} className="flex items-center gap-4 py-4"><span className="grid h-8 w-8 place-items-center bg-paper-100 text-xs font-bold text-ink-500">0{index + 1}</span><div className="min-w-0 flex-1"><p className="truncate text-sm font-bold">{prompt.title}</p><p className="mt-1 text-xs text-ink-500">{prompt.sales} uses · {prompt.rating} rating</p></div><p className="text-sm font-bold text-ink-800">₹{[5120, 3680, 1940][index]}</p></div>)}</div></div><div className="border border-ink-800 bg-ink-900 p-6 text-paper-50"><BarChart3 className="h-5 w-5 text-forge-300" /><p className="eyebrow mt-7 text-forge-300">A useful signal</p><p className="display mt-3 text-3xl text-paper-50">Your strategy brief is getting saved more than it is bought.</p><p className="mt-4 text-sm leading-6 text-ink-100">Consider adding a short, protected demo so visitors can feel its structure before committing.</p><button className="button-quiet mt-7 border-paper-50 text-paper-50 hover:border-paper-50">View listing</button></div></section></div>; }
